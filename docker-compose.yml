services:
  files:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./files-data:/usr/share/nginx/html:ro
      - ./nginx-files.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
